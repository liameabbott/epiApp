runtime: python27
api_version: 1
threadsafe: true

handlers:
- url: /static
  static_dir: static
- url: /.*
  script: main.app

libraries:
- name: MySQLdb
  version: "latest"

env_variables:
    CLOUDSQL_CONNECTION_NAME: "localhost" 
    #CLOUDSQL_CONNECTION_NAME: "epi-db-instance"
    CLOUDSQL_USER: "root"
    CLOUDSQL_PASSWORD: "thisismydb"
    #CLOUDSQL_PASSWORD: "6G9CMxAKBwb1J4PD"
